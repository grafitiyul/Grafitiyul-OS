// Duration display — moved to shared/duration.mjs so the server (confirmation
// email composer) uses the SAME wording. This module stays as the client entry.
export { durationHe, durationEn, durationDisplay } from '../../../shared/duration.mjs';
