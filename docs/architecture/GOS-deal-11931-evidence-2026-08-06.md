# Deal #11931 — evidence before a silent-WON correction

**Date:** 2026-08-06 · **Source:** production database, read-only
**Status: reported, NOT yet mutated** — the correction is pending an owner
decision on the WON date (see §5).

## 1. Identity

| | |
|---|---|
| Deal | `#11931` — "Fernandez" (internal CRM title) |
| Organization | Boker tours |
| Primary contact | אסף בוקר |
| Lead source | טופס הזמנה סוכנויות/חברות הפקה |
| Created in GOS | 2026-07-16 — i.e. by the legacy Pipedrive migration, not by a human |
| Last real activity | 2023-09-05 |

## 2. Current lifecycle

| | |
|---|---|
| Status | **OPEN** |
| Stage | סגירה (`closing`, sortOrder 5) — the last stage before the finish line |
| `wonAt` / `lostAt` | null / null |
| `wonActor` / `wonQuoteRef` | null / null |

So: the deal reached the closing stage and simply stopped. Nothing ever
recorded an outcome.

## 3. Date / tour fields, and whether a tour already exists

| | |
|---|---|
| `tourDate` / `tourTime` | **2023-07-18 16:00** |
| `participants` | 13 |
| `activityType` | business |
| `productId` / `productVariantId` / `locationId` | **all null** |
| `tourLanguage` / `durationHours` | null / null |
| Bookings | **0** |
| TourEvent | **none** |
| TicketRegistrations | **0** |

**Would creating a tour now duplicate reality?** No — it would *invent* it.
There is no historical TourEvent to duplicate, but there is also nothing to
reconstruct: the deal has no product, no variant and no location, so the
canonical WON gate for a business deal cannot even be satisfied. A tour created
today would be a 2023 record that GOS never operated, with no roster, no guide
and no components. It would be fiction, not history.

## 4. Payment and document evidence

| | |
|---|---|
| Deal value | ₪1,404 (ILS) |
| PaymentRequests | **0** |
| Payment links | **0** |
| iCount documents | **0** |
| Collection evidence rows | **0** |
| `paymentReviewStatus` / `collectionReviewStatus` | null / null |
| Quote versions | 1, `sourceKind: pipedrive_import`, not selected, not working |
| Quote documents | 0 |
| Timeline | 2 notes, both from September 2023 |
| Open בקרה issues | none |

**GOS holds no evidence that this deal was paid.** That is consistent with the
owner's account — the tour ran and was paid in 2023, in the *old* system, and
only the deal record was migrated. It is equally consistent with the rule the
correction must obey: **we do not manufacture the missing evidence.** No
payment row, no accounting document and no collection state may be created to
make the record "look" settled.

There is one open auto-task on the deal, "דיל להמשך טיפול", due 2026-08-04 —
generated by the follow-up worker precisely because the deal is still OPEN.
Closing it correctly is what makes that task stop.

## 5. The correction the evidence supports

| Setting | Value | Why |
|---|---|---|
| Status | → WON | The business outcome is known; only the record is wrong. |
| Confirmation email | **off** | A 2023 customer must not receive a 2026 email. |
| Create a TourEvent | **off** | §3: it would invent history, and the planning gate cannot be met anyway. Recorded as an *intentional* historical no-tour (`Deal.historicalWonAt`), so בקרה does not raise a false critical. |
| Payment / documents / collection | **untouched** | §4. A financial correction is a separate, explicit act. |
| WON date | **owner decision** | Two defensible dates — see below. |

**The one open question — which date.** The dialog defaults to the deal's own
tour date because that is the day the business actually happened, but the
evidence supports two readings and this is a permanent historical stamp:

* **2023-07-18** — the tour date. "The deal closed when the tour ran."
* **2023-09-05** — the last real activity on the deal in the old system.
  "The deal closed when work on it stopped."

Not guessed here. The correction is applied once the owner picks.

### Known consequence to state plainly

Once WON, the deal's ₪1,404 enters the collection module's **full** accounting
picture (the "הכול" queue) as uncollected, because GOS genuinely holds no
payment evidence for it. It does **not** enter the default "בגבייה פעילה" work
queue — that filters on `collectionReviewStatus = 'active_collection'`, and
this deal's is null. If the owner wants it marked as historically settled, the
existing `likely_paid_legacy` classification is the right, separate, explicit
tool — not something this correction should do silently.
