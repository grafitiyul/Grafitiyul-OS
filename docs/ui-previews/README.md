# UI previews — Main Products redesign

Before/after of **Settings → CRM → Products** (`ProductsSettings.jsx`).

- `products-before.png` — the previous CRUD-style list.
- `products-after.png` — the premium redesign (this pass).

These are **design previews**: the actual component markup rendered against the
real built Tailwind CSS with sample data (headless Chrome). They are not live-data
captures — the production page with real data is on Railway after deploy.

UI-only change: no data model, routes, business logic, backend fields, or API
contracts were modified.
