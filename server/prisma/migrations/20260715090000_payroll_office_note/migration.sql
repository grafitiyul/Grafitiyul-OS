-- Official office note on a payroll entry ("הערת המשרד") — a separate concept
-- from the guide↔office conversation thread and from line notes. Additive.
ALTER TABLE "PayrollEntry" ADD COLUMN "officeNote" TEXT;
